для хранения конфигов

json, xml, ini файлы   - гораздо медленннее, чем
простой php с return

поэтому их в реальном продакшене никто не применяет,
только если вам требуется чужой файл, который надо прочитать - тогда да,
есть функции parse_ini_file, xml декодеры, json loader